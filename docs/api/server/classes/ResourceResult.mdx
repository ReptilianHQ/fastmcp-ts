---
title: "ResourceResult"
---

Defined in: [server/resource.ts:56](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/resource.ts#L56)

Escape hatch for full control over resource response content.
Return this from a resource handler to specify content directly.

## Constructors

### Constructor

```ts
new ResourceResult(contents): ResourceResult;
```

Defined in: [server/resource.ts:57](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/resource.ts#L57)

#### Parameters

##### contents

\{
  `_meta?`: `Record`&lt;`string`, `unknown`&gt;;
  `blob?`: `string`;
  `mimeType?`: `string`;
  `text?`: `string`;
  `uri`: `string`;
\}[]

#### Returns

`ResourceResult`

## Properties

### contents

```ts
readonly contents: {
  _meta?: Record<string, unknown>;
  blob?: string;
  mimeType?: string;
  text?: string;
  uri: string;
}[];
```

Defined in: [server/resource.ts:58](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/resource.ts#L58)

#### \_meta?

```ts
optional _meta?: Record<string, unknown>;
```

#### blob?

```ts
optional blob?: string;
```

#### mimeType?

```ts
optional mimeType?: string;
```

#### text?

```ts
optional text?: string;
```

#### uri

```ts
uri: string;
```
