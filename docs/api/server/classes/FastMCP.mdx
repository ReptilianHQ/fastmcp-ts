---
title: "FastMCP"
---

Defined in: [server/FastMCP.ts:410](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L410)

## Constructors

### Constructor

```ts
new FastMCP(options): FastMCP;
```

Defined in: [server/FastMCP.ts:464](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L464)

#### Parameters

##### options

[`FastMCPOptions`](/api/server/interfaces/FastMCPOptions)

#### Returns

`FastMCP`

## Properties

### name

```ts
readonly name: string;
```

Defined in: [server/FastMCP.ts:411](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L411)

***

### version

```ts
readonly version: string;
```

Defined in: [server/FastMCP.ts:412](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L412)

## Accessors

### address

#### Get Signature

```ts
get address(): ServerAddress | null;
```

Defined in: [server/FastMCP.ts:1833](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1833)

The bound address after run() resolves for the http transport. Null for stdio or before run().

##### Returns

[`ServerAddress`](/api/server/interfaces/ServerAddress) \| `null`

***

### isRunning

#### Get Signature

```ts
get isRunning(): boolean;
```

Defined in: [server/FastMCP.ts:1842](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1842)

Whether `run()` or `connect()` has been called on this instance. Used by the
CLI's entrypoint loader to detect servers that already started themselves via
top-level code, so it doesn't attempt to start them a second time.

##### Returns

`boolean`

## Methods

### \_addCloseCallback()

```ts
_addCloseCallback(cb): void;
```

Defined in: [server/FastMCP.ts:1666](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1666)

Register a callback invoked when this server is closed — used by proxy connections.

#### Parameters

##### cb

() => `Promise`&lt;`void`&gt;

#### Returns

`void`

***

### \_dispatchPrompt()

```ts
_dispatchPrompt(
   name, 
   args, 
   ctx): Promise<
  | InputRequiredResult
  | {
  description?: string;
  messages: PromptMessage[];
}>;
```

Defined in: [server/FastMCP.ts:1526](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1526)

Dispatch a prompt render through this server's middleware chain using an inherited context.

#### Parameters

##### name

`string`

##### args

`Record`&lt;`string`, `string`&gt;

##### ctx

[`McpContext`](/api/server/interfaces/McpContext)

#### Returns

`Promise`&lt;
  \| `InputRequiredResult`
  \| \{
  `description?`: `string`;
  `messages`: [`PromptMessage`](/api/server/interfaces/PromptMessage)[];
\}&gt;

***

### \_dispatchResource()

```ts
_dispatchResource(
   uri, 
   params, 
   ctx): Promise<unknown>;
```

Defined in: [server/FastMCP.ts:1486](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1486)

Run a resource read through this server's middleware chain using an inherited context.
Returns the raw handler result so the parent can apply convertResourceResult with the
actual requested URI (important for template resources).

#### Parameters

##### uri

`string`

##### params

`Record`&lt;`string`, `string`&gt; \| `undefined`

##### ctx

[`McpContext`](/api/server/interfaces/McpContext)

#### Returns

`Promise`&lt;`unknown`&gt;

***

### \_dispatchTool()

```ts
_dispatchTool(
   name, 
   rawArgs, 
   ctx): Promise<
  | InputRequiredResult
  | {
[key: string]: unknown;
}>;
```

Defined in: [server/FastMCP.ts:1427](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1427)

Dispatch a tool call through this server's middleware chain using an inherited context.
Used by parent servers to honour child-level middleware when routing mounted tool calls.

#### Parameters

##### name

`string`

##### rawArgs

`unknown`

##### ctx

[`McpContext`](/api/server/interfaces/McpContext)

#### Returns

`Promise`&lt;
  \| `InputRequiredResult`
  \| \{
\[`key`: `string`\]: `unknown`;
\}&gt;

***

### \_modernFetch()

```ts
_modernFetch(request, options?): Promise<Response>;
```

Defined in: [server/FastMCP.ts:1889](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1889)

Modern-era (2026-07-28) in-process fetch entrypoint — the `McpServerLike`
duck-type hook `fastmcp-ts/client`'s `Client` looks for when a caller pins
modern era for an in-process server (`versionNegotiation: { mode: { pin:
'2026-07-28' } } }`). Delegates directly to the same `createMcpHandler`
instance the real HTTP modern path uses (`_getModernHandler`), so this is
the identical dispatch a real network connection would get — no sockets,
same code. Modern-only (`legacy: 'reject'`): a client using `'auto'` or
`'legacy'` negotiation instead goes through `connect()` + `InMemoryTransport`
(2025-era only), unaffected by this method.

#### Parameters

##### request

`Request`

##### options?

`McpHandlerRequestOptions`

#### Returns

`Promise`&lt;`Response`&gt;

***

### \_removePrompt()

```ts
_removePrompt(name): boolean;
```

Defined in: [server/FastMCP.ts:1410](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1410)

#### Parameters

##### name

`string`

#### Returns

`boolean`

***

### \_removeResource()

```ts
_removeResource(uri): boolean;
```

Defined in: [server/FastMCP.ts:1396](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1396)

#### Parameters

##### uri

`string`

#### Returns

`boolean`

***

### \_removeTool()

```ts
_removeTool(name): boolean;
```

Defined in: [server/FastMCP.ts:1389](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1389)

#### Parameters

##### name

`string`

#### Returns

`boolean`

***

### addProvider()

```ts
addProvider(provider): this;
```

Defined in: [server/FastMCP.ts:1675](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1675)

Register a provider (FastMCPApp or FastMCP) on this server.
All tools, resources, and prompts from the provider are mounted without a prefix.
For FastMCPApp, pass provider.server; for FastMCP, pass directly.

#### Parameters

##### provider

  \| `FastMCP`
  \| \{
  `server`: `FastMCP`;
\}

#### Returns

`this`

***

### close()

```ts
close(): Promise<void>;
```

Defined in: [server/FastMCP.ts:2321](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L2321)

#### Returns

`Promise`&lt;`void`&gt;

***

### connect()

```ts
connect(transport): Promise<void>;
```

Defined in: [server/FastMCP.ts:1846](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1846)

#### Parameters

##### transport

`Transport`

#### Returns

`Promise`&lt;`void`&gt;

***

### customRoute()

```ts
customRoute(config, handler): void;
```

Defined in: [server/FastMCP.ts:1385](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1385)

Register a custom HTTP route on the listener `run()` starts — e.g. a
Kubernetes liveness/readiness probe. The handler owns the whole exchange:
no MCP auth, no DNS-rebinding guards, and no CORS headers run in front of
it. Matching is exact-path (query string ignored); methods default to GET.
Routes never serve on stdio or through `fetch()` — a framework embedding
`fetch()` owns its own routing (same posture as `dnsRebinding`).

#### Parameters

##### config

[`CustomRouteConfig`](/api/server/interfaces/CustomRouteConfig)

##### handler

[`CustomRouteHandler`](/api/server/type-aliases/CustomRouteHandler)

#### Returns

`void`

***

### fetch()

```ts
fetch(request, options?): Promise<Response>;
```

Defined in: [server/FastMCP.ts:1865](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1865)

Serve an MCP request through a web-standard, fetch-native HTTP entrypoint.

This entrypoint is always stateless and does not require `run()`: legacy
(2025-era) requests use a fresh server instance per request, while modern
(2026-07-28) requests share the instance's modern handler and event bus.
Authentication is deliberately external — `options.authInfo` is trusted and
passed unchanged into the protocol handler, where it is exposed as `ctx.auth`.
The embedding framework also owns Host/Origin validation and should abort its
carrying requests during shutdown; stateless legacy response streams follow
that request lifecycle, while `close()` terminates modern exchanges and streams.

#### Parameters

##### request

`Request`

##### options?

`McpHandlerRequestOptions`

#### Returns

`Promise`&lt;`Response`&gt;

***

### getContext()

```ts
getContext(): McpContext;
```

Defined in: [server/FastMCP.ts:1826](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1826)

#### Returns

[`McpContext`](/api/server/interfaces/McpContext)

***

### mount()

```ts
mount(child, prefix?): this;
```

Defined in: [server/FastMCP.ts:1642](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1642)

Mount a child server onto this server. All tools, resources, and prompts from the child become accessible via this server. Pass a prefix to namespace names and prevent collisions.

#### Parameters

##### child

`FastMCP`

##### prefix?

`string`

#### Returns

`this`

***

### notifyResourceUpdated()

```ts
notifyResourceUpdated(uri): void;
```

Defined in: [server/FastMCP.ts:1271](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1271)

Signal that a resource changed and any subscriber should re-read it. This is
the SINGLE change signal for resource updates across both eras — the legacy
per-session push and the modern subscriptions/listen bus are driven from here:

- Legacy connections (primary / stdio / legacy HTTP sessions) that called
  `resources/subscribe` for `uri` are pushed `notifications/resources/updated`
  directly on their live session (subscription set kept in per-session state).
- The modern handler's bus (W3) is published to as well; it does its own
  per-stream filtering, delivering only to `subscriptions/listen` streams that
  opted in to this URI. Optional chaining keeps it a no-op until the first
  modern HTTP request has actually built the handler.

Unsubscribed sessions (and the modern streams that never opted in) receive
nothing, so calling this for a URI with no subscribers is safe and cheap.

#### Parameters

##### uri

`string`

#### Returns

`void`

***

### prompt()

```ts
prompt(config, handler): void;
```

Defined in: [server/FastMCP.ts:1355](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1355)

#### Parameters

##### config

[`PromptConfig`](/api/server/interfaces/PromptConfig)

##### handler

(`args?`) => `unknown`

#### Returns

`void`

***

### resource()

```ts
resource(config, handler): void;
```

Defined in: [server/FastMCP.ts:1366](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1366)

#### Parameters

##### config

[`ResourceConfig`](/api/server/interfaces/ResourceConfig)

##### handler

(`params?`) => `unknown`

#### Returns

`void`

***

### run()

```ts
run(options?): Promise<void>;
```

Defined in: [server/FastMCP.ts:1893](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1893)

#### Parameters

##### options?

[`RunOptions`](/api/server/interfaces/RunOptions)

#### Returns

`Promise`&lt;`void`&gt;

***

### tool()

#### Call Signature

```ts
tool<S>(config, handler): void;
```

Defined in: [server/FastMCP.ts:1340](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1340)

##### Type Parameters

###### S

`S` *extends* `StandardSchemaV1`&lt;`unknown`, `unknown`&gt;

##### Parameters

###### config

`Omit`&lt;[`ToolConfig`](/api/server/interfaces/ToolConfig), `"input"`&gt; & \{
  `input`: `S`;
\}

###### handler

(`args`) => `unknown`

##### Returns

`void`

#### Call Signature

```ts
tool(config, handler): void;
```

Defined in: [server/FastMCP.ts:1345](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1345)

##### Parameters

###### config

[`ToolConfig`](/api/server/interfaces/ToolConfig)

###### handler

(`args`) => `unknown`

##### Returns

`void`

***

### transform()

```ts
transform(t): this;
```

Defined in: [server/FastMCP.ts:1418](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1418)

Add a transform to the pipeline. Applied to list responses in registration order.

#### Parameters

##### t

[`Transform`](/api/server/interfaces/Transform)

#### Returns

`this`

***

### use()

```ts
use(mw): this;
```

Defined in: [server/FastMCP.ts:1820](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L1820)

Add a middleware to the pipeline.

Can be called at any point before the first request arrives. `setup()` is
called immediately on the primary server so that notification handlers and
other server-level side-effects are active before `connect()` / `run()` is
called. HTTP sessions created after `use()` will also have `setup()` called
via `_makeServer()`.

#### Parameters

##### mw

[`Middleware`](/api/server/interfaces/Middleware)

#### Returns

`this`
