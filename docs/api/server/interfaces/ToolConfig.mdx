---
title: "ToolConfig"
---

Defined in: [server/FastMCP.ts:258](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L258)

## Properties

### annotations?

```ts
optional annotations?: ToolAnnotations;
```

Defined in: [server/FastMCP.ts:264](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L264)

Behavioral hints for clients (MCP 2025-03-26). Forwarded verbatim in tools/list.

***

### auth?

```ts
optional auth?: AuthCheck;
```

Defined in: [server/FastMCP.ts:286](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L286)

***

### description

```ts
description: string;
```

Defined in: [server/FastMCP.ts:262](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L262)

***

### disabled?

```ts
optional disabled?: boolean;
```

Defined in: [server/FastMCP.ts:283](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L283)

When true the tool is hidden from listTools and cannot be invoked via tools/call.

***

### input?

```ts
optional input?: StandardSchemaV1<unknown, unknown>;
```

Defined in: [server/FastMCP.ts:266](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L266)

Standard Schema validator for the tool's input arguments. Used for runtime validation.

***

### inputSchema?

```ts
optional inputSchema?: Record<string, unknown>;
```

Defined in: [server/FastMCP.ts:272](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L272)

Explicit JSON Schema advertised to clients as `inputSchema`. Overrides auto-generation from
`input`. Use when you need JSON Schema features beyond what your validator auto-generates
(e.g. `examples`, `$comment`, per-property descriptions, or custom annotations).

***

### name

```ts
name: string;
```

Defined in: [server/FastMCP.ts:259](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L259)

***

### output?

```ts
optional output?: StandardSchemaV1<unknown, unknown>;
```

Defined in: [server/FastMCP.ts:274](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L274)

Standard Schema validator for the tool's return value. Validated before result conversion.

***

### outputSchema?

```ts
optional outputSchema?: Record<string, unknown>;
```

Defined in: [server/FastMCP.ts:279](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L279)

Explicit JSON Schema advertised to clients as `outputSchema`. Overrides auto-generation from
`output`. Use when you need JSON Schema features beyond what your validator auto-generates.

***

### tags?

```ts
optional tags?: string[];
```

Defined in: [server/FastMCP.ts:285](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L285)

Arbitrary tags for server-side filtering.

***

### timeout?

```ts
optional timeout?: number;
```

Defined in: [server/FastMCP.ts:281](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L281)

Execution timeout in milliseconds. No timeout by default.

***

### title?

```ts
optional title?: string;
```

Defined in: [server/FastMCP.ts:261](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L261)

Human-readable display name shown in UIs. Takes precedence over `name` for display purposes.

***

### ui?

```ts
optional ui?: UiToolMeta;
```

Defined in: [server/FastMCP.ts:288](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/FastMCP.ts#L288)

Apps extension — links this tool to a UI resource and controls visibility.
