---
title: "McpContext"
---

Defined in: [server/context.ts:82](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L82)

## Properties

### auth

```ts
auth: AccessToken | undefined;
```

Defined in: [server/context.ts:84](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L84)

Auth token for the current request, if any.

***

### http

```ts
http: HttpRequestContext | undefined;
```

Defined in: [server/context.ts:97](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L97)

The HTTP request carrying the current MCP message, when serving over an
HTTP transport; `undefined` on stdio. Fresh per request: on a sessionful
connection each request's own headers appear here, never the
initialize-time ones. Credential headers are withheld by default and
listed in `http.redactedHeaderNames` (tune with `FastMCPOptions.http`).
Untrusted input; see HttpRequestContext's docs before deriving any
authorization from it.

***

### inputResponses

```ts
inputResponses: InputResponses | undefined;
```

Defined in: [server/context.ts:173](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L173)

The current round's embedded input responses, when this request is a retry of an
earlier `inputRequired(...)` return. `undefined` on the flow's first call (no
prior round to respond to). Read with the SDK's `acceptedContent(ctx.inputResponses,
key)` / `inputResponse(ctx.inputResponses, key)` (re-exported from
`fastmcp-ts/server`) rather than indexing this object directly — those readers
validate shape and, for `acceptedContent`, optionally the content against a schema.

***

### requestId

```ts
requestId: string | undefined;
```

Defined in: [server/context.ts:86](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L86)

The MCP request ID for the current call.

## Methods

### alert()

```ts
alert(message, loggerName?): Promise<void>;
```

Defined in: [server/context.ts:109](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L109)

#### Parameters

##### message

`string`

##### loggerName?

`string`

#### Returns

`Promise`&lt;`void`&gt;

***

### critical()

```ts
critical(message, loggerName?): Promise<void>;
```

Defined in: [server/context.ts:108](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L108)

#### Parameters

##### message

`string`

##### loggerName?

`string`

#### Returns

`Promise`&lt;`void`&gt;

***

### debug()

```ts
debug(message, loggerName?): Promise<void>;
```

Defined in: [server/context.ts:103](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L103)

#### Parameters

##### message

`string`

##### loggerName?

`string`

#### Returns

`Promise`&lt;`void`&gt;

***

### deleteState()

```ts
deleteState(key): void;
```

Defined in: [server/context.ts:200](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L200)

Remove a value from session-scoped state.

#### Parameters

##### key

`string`

#### Returns

`void`

***

### ~~elicit()~~

```ts
elicit(message, schema): Promise<ElicitationResult>;
```

Defined in: [server/context.ts:148](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L148)

Ask the client to collect input from the user via a form dialog.
Throws if the client has not advertised the `elicitation` capability.

#### Parameters

##### message

`string`

##### schema

[`ElicitationSchema`](/api/server/interfaces/ElicitationSchema)

#### Returns

`Promise`&lt;[`ElicitationResult`](/api/server/interfaces/ElicitationResult)&gt;

#### Deprecated

Throws a clear error naming the replacement when called on a modern
(2026-07-28) request — return `inputRequired({ inputRequests: { id:
inputRequired.elicit({...}) } })` instead (see `ctx.sample`'s docs; the same
write-once, serve-both-eras guidance applies).

***

### emergency()

```ts
emergency(message, loggerName?): Promise<void>;
```

Defined in: [server/context.ts:110](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L110)

#### Parameters

##### message

`string`

##### loggerName?

`string`

#### Returns

`Promise`&lt;`void`&gt;

***

### error()

```ts
error(message, loggerName?): Promise<void>;
```

Defined in: [server/context.ts:107](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L107)

#### Parameters

##### message

`string`

##### loggerName?

`string`

#### Returns

`Promise`&lt;`void`&gt;

***

### getState()

```ts
getState(key): unknown;
```

Defined in: [server/context.ts:196](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L196)

Retrieve a value from session-scoped state. Returns undefined if not set.

#### Parameters

##### key

`string`

#### Returns

`unknown`

***

### info()

```ts
info(message, loggerName?): Promise<void>;
```

Defined in: [server/context.ts:104](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L104)

#### Parameters

##### message

`string`

##### loggerName?

`string`

#### Returns

`Promise`&lt;`void`&gt;

***

### ~~listRoots()~~

```ts
listRoots(): Promise<Root[]>;
```

Defined in: [server/context.ts:161](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L161)

Request the list of filesystem roots the client has declared.
Throws if the client has not advertised the `roots` capability.

#### Returns

`Promise`&lt;[`Root`](/api/server/interfaces/Root)[]&gt;

#### Deprecated

Roots is deprecated as of protocol revision 2026-07-28 (SEP-2577) —
prefer passing paths via tool parameters, resource URIs, or server configuration.
Throws a clear error naming `inputRequired(...)` as the multi-round-trip
replacement when called on a modern request (see `ctx.sample`'s docs).

***

### log()

```ts
log(
   level, 
   message, 
   loggerName?): Promise<void>;
```

Defined in: [server/context.ts:102](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L102)

Send a log message to the client at the specified RFC 5424 severity level.

#### Parameters

##### level

[`LogLevel`](/api/server/type-aliases/LogLevel)

##### message

`string`

##### loggerName?

`string`

#### Returns

`Promise`&lt;`void`&gt;

***

### mintRequestState()

```ts
mintRequestState<T>(payload): Promise<string>;
```

Defined in: [server/context.ts:191](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L191)

Seals `payload` into the opaque `requestState` string to return from
`inputRequired({ requestState })`. HMAC-signed via `FastMCPOptions.requestState`
when configured; otherwise a plain `JSON.stringify(payload)` with a console
warning — the resulting state is unsigned and MUST NOT be trusted for
anything that influences authorization, resource access, or business logic (the
client can read and tamper with it) unless `FastMCPOptions.requestState` is set.

#### Type Parameters

##### T

`T` = `unknown`

#### Parameters

##### payload

`T`

#### Returns

`Promise`&lt;`string`&gt;

***

### notice()

```ts
notice(message, loggerName?): Promise<void>;
```

Defined in: [server/context.ts:105](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L105)

#### Parameters

##### message

`string`

##### loggerName?

`string`

#### Returns

`Promise`&lt;`void`&gt;

***

### onClose()

```ts
onClose(callback): void;
```

Defined in: [server/context.ts:218](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L218)

Register a callback that runs when the current session closes.
Useful for releasing per-session resources (e.g. uploaded files).
No-op if called outside an active HTTP session.

#### Parameters

##### callback

() => `void`

#### Returns

`void`

***

### reportProgress()

```ts
reportProgress(
   progress, 
   total?, 
   message?): Promise<void>;
```

Defined in: [server/context.ts:118](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L118)

Send a progress notification to the client.
No-op if the request did not include a `progressToken` in its `_meta`.

#### Parameters

##### progress

`number`

##### total?

`number`

##### message?

`string`

#### Returns

`Promise`&lt;`void`&gt;

***

### requestState()

```ts
requestState<T>(): T | undefined;
```

Defined in: [server/context.ts:181](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L181)

Reads the current round's `requestState`, already verified and decoded when
`FastMCPOptions.requestState` is configured (the payload `verify` resolved with);
the raw, unverified wire string when it is not configured — treat that case as
attacker-controlled input. `undefined` when the round carried no state.

#### Type Parameters

##### T

`T` = `unknown`

#### Returns

`T` \| `undefined`

***

### resolveToolName()

```ts
resolveToolName(name): string;
```

Defined in: [server/context.ts:209](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L209)

Resolve a logical tool name to its current external name, accounting for
any namespace prefix applied when the owning FastMCPApp was mounted.
Returns the name unchanged when called outside a mounted context.

#### Parameters

##### name

`string`

#### Returns

`string`

***

### ~~sample()~~

```ts
sample(params): Promise<SamplingResult>;
```

Defined in: [server/context.ts:135](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L135)

Ask the client to perform an LLM inference call and return the result.
Throws if the client has not advertised the `sampling` capability.

#### Parameters

##### params

[`SamplingParams`](/api/server/interfaces/SamplingParams)

#### Returns

`Promise`&lt;[`SamplingResult`](/api/server/interfaces/SamplingResult)&gt;

#### Deprecated

Sampling is deprecated as of protocol revision 2026-07-28 (SEP-2577)
and this push-style call only works on a legacy (2025-era) connection — calling it
on a modern request throws a clear error naming the replacement (return
`inputRequired({ inputRequests: { id: inputRequired.createMessage({...}) } })` from
the handler instead; see `fastmcp-ts/server`'s re-exported `inputRequired`). A
handler written the `inputRequired` way serves both eras unchanged — the SDK's
legacy shim fulfils it via a real server→client request on 2025-era connections, so
there is rarely a reason to keep calling `ctx.sample()` directly in new code.

***

### setState()

```ts
setState(key, value): void;
```

Defined in: [server/context.ts:198](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L198)

Store a value in session-scoped state. Persists for the lifetime of the session.

#### Parameters

##### key

`string`

##### value

`unknown`

#### Returns

`void`

***

### warning()

```ts
warning(message, loggerName?): Promise<void>;
```

Defined in: [server/context.ts:106](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/context.ts#L106)

#### Parameters

##### message

`string`

##### loggerName?

`string`

#### Returns

`Promise`&lt;`void`&gt;
