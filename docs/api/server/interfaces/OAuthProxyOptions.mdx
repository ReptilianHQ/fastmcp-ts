---
title: "OAuthProxyOptions"
---

Defined in: [server/auth/oauth/proxy.ts:4](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/auth/oauth/proxy.ts#L4)

## Properties

### endpoints

```ts
endpoints: {
  authorizationUrl: string;
  revocationUrl?: string;
  tokenUrl: string;
};
```

Defined in: [server/auth/oauth/proxy.ts:11](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/auth/oauth/proxy.ts#L11)

Upstream OAuth server endpoint URLs.

#### authorizationUrl

```ts
authorizationUrl: string;
```

#### revocationUrl?

```ts
optional revocationUrl?: string;
```

#### tokenUrl

```ts
tokenUrl: string;
```

***

### upstreamCredentials

```ts
upstreamCredentials: {
  clientId: string;
  clientSecret?: string;
};
```

Defined in: [server/auth/oauth/proxy.ts:6](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/auth/oauth/proxy.ts#L6)

Pre-registered credentials this proxy uses when talking to the upstream server.

#### clientId

```ts
clientId: string;
```

#### clientSecret?

```ts
optional clientSecret?: string;
```

***

### verifyAccessToken

```ts
verifyAccessToken: (token) => Promise<AuthInfo>;
```

Defined in: [server/auth/oauth/proxy.ts:20](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/server/auth/oauth/proxy.ts#L20)

Verifies an access token issued by the upstream and returns information about it.
Called for every MCP request to validate the bearer token.

#### Parameters

##### token

`string`

#### Returns

`Promise`&lt;`AuthInfo`&gt;
