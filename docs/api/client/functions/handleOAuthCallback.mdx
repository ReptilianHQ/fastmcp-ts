---
title: "handleOAuthCallback"
---

```ts
function handleOAuthCallback(opts?): void;
```

Defined in: [client/browser-oauth.ts:129](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/browser-oauth.ts#L129)

Call from the redirect_uri page (popup mode) to deliver the authorization
code back to the opener window and close the popup.

## Parameters

### opts?

#### targetOrigin?

`string`

## Returns

`void`
