---
title: "Client"
---

Defined in: [client/client.ts:72](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L72)

## Implements

- [`IClient`](/api/client/interfaces/IClient)

## Constructors

### Constructor

```ts
new Client(input, options?): Client;
```

Defined in: [client/client.ts:117](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L117)

#### Parameters

##### input

[`ClientTransportInput`](/api/client/type-aliases/ClientTransportInput)

##### options?

[`ClientOptions`](/api/client/interfaces/ClientOptions)

#### Returns

`Client`

## Methods

### \[asyncDispose\]()

```ts
asyncDispose: Promise<void>;
```

Defined in: [client/client.ts:278](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L278)

#### Returns

`Promise`&lt;`void`&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`[asyncDispose]`](/api/client/interfaces/IClient#asyncdispose)

***

### callTool()

```ts
callTool<TData>(
   name, 
   args?, 
   options?): Promise<CallToolResult<TData>>;
```

Defined in: [client/client.ts:385](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L385)

#### Type Parameters

##### TData

`TData` = `unknown`

#### Parameters

##### name

`string`

##### args?

`Record`&lt;`string`, `unknown`&gt;

##### options?

[`CallToolOptions`](/api/client/interfaces/CallToolOptions)

#### Returns

`Promise`&lt;[`CallToolResult`](/api/client/type-aliases/CallToolResult)&lt;`TData`&gt;&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`callTool`](/api/client/interfaces/IClient#calltool)

***

### callToolRaw()

```ts
callToolRaw<TData>(
   name, 
   args?, 
   options?): Promise<CallToolResult<TData>>;
```

Defined in: [client/client.ts:403](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L403)

Returns the full result including isError without throwing.

#### Type Parameters

##### TData

`TData` = `unknown`

#### Parameters

##### name

`string`

##### args?

`Record`&lt;`string`, `unknown`&gt;

##### options?

[`CallToolOptions`](/api/client/interfaces/CallToolOptions)

#### Returns

`Promise`&lt;[`CallToolResult`](/api/client/type-aliases/CallToolResult)&lt;`TData`&gt;&gt;

***

### close()

```ts
close(): Promise<void>;
```

Defined in: [client/client.ts:261](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L261)

#### Returns

`Promise`&lt;`void`&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`close`](/api/client/interfaces/IClient#close)

***

### complete()

```ts
complete(
   ref, 
   argument, 
   context?, 
   options?): Promise<CompletionResult>;
```

Defined in: [client/client.ts:576](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L576)

#### Parameters

##### ref

  \| \{
  `name`: `string`;
  `type`: `"ref/prompt"`;
\}
  \| \{
  `type`: `"ref/resource"`;
  `uri`: `string`;
\}

##### argument

###### name

`string`

###### value

`string`

##### context?

###### arguments?

`Record`&lt;`string`, `string`&gt;

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;[`CompletionResult`](/api/client/type-aliases/CompletionResult)&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`complete`](/api/client/interfaces/IClient#complete)

***

### connect()

```ts
connect(): Promise<void>;
```

Defined in: [client/client.ts:150](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L150)

#### Returns

`Promise`&lt;`void`&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`connect`](/api/client/interfaces/IClient#connect)

***

### getInstructions()

```ts
getInstructions(): string | undefined;
```

Defined in: [client/client.ts:358](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L358)

The server's `instructions` string, when the server provided one — from
the legacy `initialize` result or, on a modern (2026-07-28) connection,
from the `server/discover` result. `undefined` otherwise and before
connect().

#### Returns

`string` \| `undefined`

***

### getPrompt()

```ts
getPrompt(
   name, 
   args?, 
   options?): Promise<{
[key: string]: unknown;
}>;
```

Defined in: [client/client.ts:558](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L558)

#### Parameters

##### name

`string`

##### args?

`Record`&lt;`string`, `string`&gt;

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;\{
\[`key`: `string`\]: `unknown`;
\}&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`getPrompt`](/api/client/interfaces/IClient#getprompt)

***

### getProtocolEra()

```ts
getProtocolEra(): ProtocolEra | undefined;
```

Defined in: [client/client.ts:335](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L335)

The protocol era negotiated at connect(): `'modern'` for 2026-07-28 (per-request
envelope), `'legacy'` for 2025-11-25 and earlier (the `initialize` handshake).
`undefined` before connect(). See `ClientOptions.versionNegotiation`.

#### Returns

`ProtocolEra` \| `undefined`

***

### getServerCapabilities()

```ts
getServerCapabilities(): 
  | {
}
  | undefined;
```

Defined in: [client/client.ts:367](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L367)

The capabilities the server advertised during connection — on the legacy
`initialize` result, or on the `server/discover` result for a modern
(2026-07-28) connection. `undefined` before connect().

#### Returns

  \| \{
\}
  \| `undefined`

***

### getServerInfo()

```ts
getServerInfo(): 
  | {
}
  | undefined;
```

Defined in: [client/client.ts:348](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L348)

The connected server's self-reported identity (`serverInfo`: name, version,
and optional title / websiteUrl / icons), when it identified itself. On a
legacy connection it comes from the `initialize` result, where it is
required. On a modern (2026-07-28) connection it comes from the
`server/discover` result's metadata — FastMCP servers advertise it there,
but an anonymous modern server may omit it, leaving this `undefined`.
`undefined` before connect().

#### Returns

  \| \{
\}
  \| `undefined`

***

### isConnected()

```ts
isConnected(): boolean;
```

Defined in: [client/client.ts:282](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L282)

#### Returns

`boolean`

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`isConnected`](/api/client/interfaces/IClient#isconnected)

***

### listPrompts()

```ts
listPrompts(options?): Promise<{
}[]>;
```

Defined in: [client/client.ts:548](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L548)

#### Parameters

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;\{
\}[]&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`listPrompts`](/api/client/interfaces/IClient#listprompts)

***

### listResources()

```ts
listResources(options?): Promise<{
}[]>;
```

Defined in: [client/client.ts:423](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L423)

#### Parameters

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;\{
\}[]&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`listResources`](/api/client/interfaces/IClient#listresources)

***

### listResourceTemplates()

```ts
listResourceTemplates(options?): Promise<{
}[]>;
```

Defined in: [client/client.ts:433](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L433)

#### Parameters

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;\{
\}[]&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`listResourceTemplates`](/api/client/interfaces/IClient#listresourcetemplates)

***

### listTools()

```ts
listTools(options?): Promise<{
}[]>;
```

Defined in: [client/client.ts:375](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L375)

#### Parameters

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;\{
\}[]&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`listTools`](/api/client/interfaces/IClient#listtools)

***

### notifyRootsChanged()

```ts
notifyRootsChanged(): Promise<void>;
```

Defined in: [client/client.ts:847](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L847)

Notify the connected server that the client's roots list has changed.
The server should re-issue a roots/list request to get the updated list.

#### Returns

`Promise`&lt;`void`&gt;

***

### ping()

```ts
ping(options?): Promise<boolean>;
```

Defined in: [client/client.ts:321](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L321)

Liveness check. On a legacy-era connection this sends the `ping` RPC. On a
modern (2026-07-28) connection `ping` is not a wire method — the era's
registry deliberately excludes it — so this sends `server/discover`
instead: any successful response is equally strong liveness evidence, and
`discover()` is the SDK-native modern equivalent (rather than a
hand-rolled substitute). Returns `false` only on legacy `ping`'s own
result; a modern-era failure throws, matching `ping()`'s existing
throw-on-failure contract.

#### Parameters

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;`boolean`&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`ping`](/api/client/interfaces/IClient#ping)

***

### readResource()

```ts
readResource(uri, options?): Promise<(
  | {
}
  | {
})[]>;
```

Defined in: [client/client.ts:443](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L443)

#### Parameters

##### uri

`string`

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;(
  \| \{
\}
  \| \{
\})[]&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`readResource`](/api/client/interfaces/IClient#readresource)

***

### readResourceRaw()

```ts
readResourceRaw(uri, options?): Promise<{
[key: string]: unknown;
}>;
```

Defined in: [client/client.ts:457](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L457)

Returns the raw SDK ReadResourceResult without unwrapping.

#### Parameters

##### uri

`string`

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;\{
\[`key`: `string`\]: `unknown`;
\}&gt;

***

### setLogLevel()

```ts
setLogLevel(level, options?): Promise<void>;
```

Defined in: [client/client.ts:605](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L605)

Sets the minimum severity level for log messages sent by the server. On a
legacy-era connection this sends the `logging/setLevel` RPC, unchanged. On
a modern (2026-07-28) connection `logging/setLevel` is deprecated (SEP-2577)
and physically absent from the era's registry — there is no RPC to send.
Instead, the level is recorded and threaded into
`_meta['io.modelcontextprotocol/logLevel']` on every subsequent request via
`_metaParams()`, per request; user-supplied `_meta` still wins if a caller
passes one explicitly (there is no such call site in this class today).

#### Parameters

##### level

`LoggingLevel`

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;`void`&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`setLogLevel`](/api/client/interfaces/IClient#setloglevel)

***

### subscribeResource()

```ts
subscribeResource(
   uri, 
   handler, 
   options?): Promise<void>;
```

Defined in: [client/client.ts:475](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L475)

Subscribes to change notifications for a resource URI. `handler` fires whenever
the server sends a matching update, on either protocol era: on a legacy
(2025-era) connection via the `resources/subscribe` RPC and unsolicited
`notifications/resources/updated` push, or on a modern (2026-07-28) connection
via a `subscriptions/listen` stream opted into `resourceSubscriptions` — both
dispatch to the same `notifications/resources/updated` handler registered in
`_registerHandlers`, so this method is the only era-aware part.

#### Parameters

##### uri

`string`

##### handler

[`ResourceUpdateHandler`](/api/client/type-aliases/ResourceUpdateHandler)

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;`void`&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`subscribeResource`](/api/client/interfaces/IClient#subscriberesource)

***

### unsubscribeResource()

```ts
unsubscribeResource(uri, options?): Promise<void>;
```

Defined in: [client/client.ts:493](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L493)

#### Parameters

##### uri

`string`

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;`void`&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`unsubscribeResource`](/api/client/interfaces/IClient#unsubscriberesource)

***

### connect()

#### Call Signature

```ts
static connect(input, options?): Promise<MultiServerClient>;
```

Defined in: [client/client.ts:287](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L287)

Creates a connected MultiServerClient when given a multi-entry McpConfig.

##### Parameters

###### input

[`McpConfig`](/api/client/type-aliases/McpConfig)

###### options?

[`MultiServerOptions`](/api/client/interfaces/MultiServerOptions)

##### Returns

`Promise`&lt;[`MultiServerClient`](/api/client/classes/MultiServerClient)&gt;

#### Call Signature

```ts
static connect(input, options?): Promise<Client>;
```

Defined in: [client/client.ts:289](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/client.ts#L289)

Creates a connected Client for a single-server transport. Use with `await using` for automatic cleanup.

##### Parameters

###### input

[`ClientTransportInput`](/api/client/type-aliases/ClientTransportInput)

###### options?

[`ClientOptions`](/api/client/interfaces/ClientOptions)

##### Returns

`Promise`&lt;`Client`&gt;
