---
title: "MultiServerClient"
---

Defined in: [client/multi-server.ts:56](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L56)

## Implements

- [`IClient`](/api/client/interfaces/IClient)

## Constructors

### Constructor

```ts
new MultiServerClient(config, options?): MultiServerClient;
```

Defined in: [client/multi-server.ts:87](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L87)

#### Parameters

##### config

[`McpConfig`](/api/client/type-aliases/McpConfig)

##### options?

[`MultiServerOptions`](/api/client/interfaces/MultiServerOptions)

#### Returns

`MultiServerClient`

## Methods

### \[asyncDispose\]()

```ts
asyncDispose: Promise<void>;
```

Defined in: [client/multi-server.ts:171](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L171)

#### Returns

`Promise`&lt;`void`&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`[asyncDispose]`](/api/client/interfaces/IClient#asyncdispose)

***

### callTool()

```ts
callTool<TData>(
   name, 
   args?, 
   options?): Promise<CallToolResult<TData>>;
```

Defined in: [client/multi-server.ts:237](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L237)

#### Type Parameters

##### TData

`TData` = `unknown`

#### Parameters

##### name

`string`

##### args?

`Record`&lt;`string`, `unknown`&gt;

##### options?

[`CallToolOptions`](/api/client/interfaces/CallToolOptions)

#### Returns

`Promise`&lt;[`CallToolResult`](/api/client/type-aliases/CallToolResult)&lt;`TData`&gt;&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`callTool`](/api/client/interfaces/IClient#calltool)

***

### callToolRaw()

```ts
callToolRaw<TData>(
   name, 
   args?, 
   options?): Promise<CallToolResult<TData>>;
```

Defined in: [client/multi-server.ts:254](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L254)

#### Type Parameters

##### TData

`TData` = `unknown`

#### Parameters

##### name

`string`

##### args?

`Record`&lt;`string`, `unknown`&gt;

##### options?

[`CallToolOptions`](/api/client/interfaces/CallToolOptions)

#### Returns

`Promise`&lt;[`CallToolResult`](/api/client/type-aliases/CallToolResult)&lt;`TData`&gt;&gt;

***

### close()

```ts
close(): Promise<void>;
```

Defined in: [client/multi-server.ts:157](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L157)

#### Returns

`Promise`&lt;`void`&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`close`](/api/client/interfaces/IClient#close)

***

### complete()

```ts
complete(
   ref, 
   argument, 
   context?, 
   options?): Promise<CompletionResult>;
```

Defined in: [client/multi-server.ts:491](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L491)

#### Parameters

##### ref

  \| \{
  `name`: `string`;
  `type`: `"ref/prompt"`;
\}
  \| \{
  `type`: `"ref/resource"`;
  `uri`: `string`;
\}

##### argument

###### name

`string`

###### value

`string`

##### context?

###### arguments?

`Record`&lt;`string`, `string`&gt;

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;[`CompletionResult`](/api/client/type-aliases/CompletionResult)&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`complete`](/api/client/interfaces/IClient#complete)

***

### connect()

```ts
connect(): Promise<void>;
```

Defined in: [client/multi-server.ts:109](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L109)

#### Returns

`Promise`&lt;`void`&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`connect`](/api/client/interfaces/IClient#connect)

***

### getPrompt()

```ts
getPrompt(
   name, 
   args?, 
   options?): Promise<{
[key: string]: unknown;
}>;
```

Defined in: [client/multi-server.ts:372](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L372)

#### Parameters

##### name

`string`

##### args?

`Record`&lt;`string`, `string`&gt;

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;\{
\[`key`: `string`\]: `unknown`;
\}&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`getPrompt`](/api/client/interfaces/IClient#getprompt)

***

### getProtocolEra()

```ts
getProtocolEra(serverName): ProtocolEra | undefined;
```

Defined in: [client/multi-server.ts:201](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L201)

The protocol era negotiated with a specific named server: `'modern'` for
2026-07-28, `'legacy'` for 2025-11-25 and earlier, `undefined` if the
server name is unknown or not yet connected. See
`MultiServerOptions.versionNegotiation` — the same mode is requested of
every server, but each negotiates (and may land on) its own era
independently, so this is necessarily per-server rather than a single
client-wide value.

#### Parameters

##### serverName

`string`

#### Returns

`ProtocolEra` \| `undefined`

***

### isConnected()

```ts
isConnected(): boolean;
```

Defined in: [client/multi-server.ts:175](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L175)

#### Returns

`boolean`

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`isConnected`](/api/client/interfaces/IClient#isconnected)

***

### listPrompts()

```ts
listPrompts(options?): Promise<{
}[]>;
```

Defined in: [client/multi-server.ts:358](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L358)

#### Parameters

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;\{
\}[]&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`listPrompts`](/api/client/interfaces/IClient#listprompts)

***

### listResources()

```ts
listResources(options?): Promise<{
}[]>;
```

Defined in: [client/multi-server.ts:277](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L277)

#### Parameters

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;\{
\}[]&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`listResources`](/api/client/interfaces/IClient#listresources)

***

### listResourceTemplates()

```ts
listResourceTemplates(options?): Promise<{
}[]>;
```

Defined in: [client/multi-server.ts:298](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L298)

#### Parameters

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;\{
\}[]&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`listResourceTemplates`](/api/client/interfaces/IClient#listresourcetemplates)

***

### listTools()

```ts
listTools(options?): Promise<{
}[]>;
```

Defined in: [client/multi-server.ts:223](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L223)

#### Parameters

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;\{
\}[]&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`listTools`](/api/client/interfaces/IClient#listtools)

***

### ping()

```ts
ping(options?): Promise<boolean>;
```

Defined in: [client/multi-server.ts:207](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L207)

See Client.ping's docs — the same era-routing applies here, per server,
since each connected server negotiates its own era independently.

#### Parameters

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;`boolean`&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`ping`](/api/client/interfaces/IClient#ping)

***

### readResource()

```ts
readResource(uri, options?): Promise<(
  | {
}
  | {
})[]>;
```

Defined in: [client/multi-server.ts:319](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L319)

#### Parameters

##### uri

`string`

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;(
  \| \{
\}
  \| \{
\})[]&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`readResource`](/api/client/interfaces/IClient#readresource)

***

### setLogLevel()

```ts
setLogLevel(level, options?): Promise<void>;
```

Defined in: [client/multi-server.ts:548](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L548)

See Client.setLogLevel's docs — the same era-routing applies here, per
server: legacy sends the real `logging/setLevel` RPC; modern records the
level and threads it into `_meta` on that server's subsequent requests via
`_metaParamsFor`, since `logging/setLevel` is absent from the modern wire
registry (SEP-2577).

#### Parameters

##### level

`LoggingLevel`

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;`void`&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`setLogLevel`](/api/client/interfaces/IClient#setloglevel)

***

### subscribeResource()

```ts
subscribeResource(
   uri, 
   handler, 
   options?): Promise<void>;
```

Defined in: [client/multi-server.ts:392](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L392)

See Client.subscribeResource's docs — the same era-routing applies here,
per server, since each connected server negotiates its own era.

#### Parameters

##### uri

`string`

##### handler

[`ResourceUpdateHandler`](/api/client/type-aliases/ResourceUpdateHandler)

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;`void`&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`subscribeResource`](/api/client/interfaces/IClient#subscriberesource)

***

### unsubscribeResource()

```ts
unsubscribeResource(uri, options?): Promise<void>;
```

Defined in: [client/multi-server.ts:434](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L434)

#### Parameters

##### uri

`string`

##### options?

[`RequestOptions`](/api/client/interfaces/RequestOptions)

#### Returns

`Promise`&lt;`void`&gt;

#### Implementation of

[`IClient`](/api/client/interfaces/IClient).[`unsubscribeResource`](/api/client/interfaces/IClient#unsubscriberesource)

***

### connect()

```ts
static connect(config, options?): Promise<MultiServerClient>;
```

Defined in: [client/multi-server.ts:179](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/multi-server.ts#L179)

#### Parameters

##### config

[`McpConfig`](/api/client/type-aliases/McpConfig)

##### options?

[`MultiServerOptions`](/api/client/interfaces/MultiServerOptions)

#### Returns

`Promise`&lt;`MultiServerClient`&gt;
