---
title: "OnTokenCallback"
---

```ts
type OnTokenCallback = (token) => void;
```

Defined in: [client/sampling/types.ts:13](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/sampling/types.ts#L13)

Called with each text token as it is streamed from the provider.

## Parameters

### token

`string`

## Returns

`void`
