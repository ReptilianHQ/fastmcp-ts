---
title: "CallToolResult"
---

```ts
type CallToolResult<TData> = SdkCallToolResult & {
  isError: boolean;
  structuredContent: TData | null;
};
```

Defined in: [client/results.ts:36](https://github.com/PrefectHQ/fastmcp-ts/blob/main/src/client/results.ts#L36)

The SDK's CallToolResult with a typed generic for structuredContent.
The intersection retains its open extension fields while narrowing the two
values FastMCP normalizes.

## Type Declaration

### isError

```ts
isError: boolean;
```

### structuredContent

```ts
structuredContent: TData | null;
```

## Type Parameters

### TData

`TData` = `unknown`
